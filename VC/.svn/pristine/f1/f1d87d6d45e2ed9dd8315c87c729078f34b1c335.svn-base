storeCommon = {
	isRepeat : function(title,ary) {
		var nary = ary.sort();
		if(nary.length>1){
			for(var i=0;i<nary.length-1;i++) {
				if (nary[i].value==nary[i+1].value) {
					alert(title+"[" + nary[i].value+"]");
					return true;
				}
			}		
		}
		return false;
	}
}